export const metadata = {
    title: "Notes — IDEXV",
    description: "Notes, scripts, findings, etc."
}

export default function NotesPage() {
    return (
        <>
            <p>Work in Progress</p>
        </>
    )
}